let nums = [5,6,2,4,9,6,2];

console.log(nums.indexOf(2)); // procura onde no array o 2 aparece pela primeira vez (em que posição)

console.log(nums.lastIndexOf(2)); // procura a ultima vez que o 2 aparece no array